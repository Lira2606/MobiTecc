import { LoginForm } from '@/components/login-form';

export default function LoginPage() {
  return (
    <div className="flex flex-col flex-grow h-full p-6 justify-center">
      <LoginForm />
    </div>
  );
}
